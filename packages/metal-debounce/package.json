{
  "name": "metal-debounce",
  "version": "2.0.0",
  "description": "Metal's debounce utility",
  "license": "BSD",
  "repository": "metal/metal-debounce",
  "engines": {
    "node": ">=0.12.0",
    "npm": ">=3.0.0"
  },
  "jsnext:main": "src/debounce.js",
  "main": "lib/debounce.js",
  "files": [
    "lib",
    "src/**/*.js",
    "test/**/*.js"
  ],
  "scripts": {
    "compile": "babel --presets env -d lib/ src/",
    "prepublish": "npm run compile",
    "test": "gulp test"
  },
  "keywords": [
    "metal"
  ],
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-preset-env": "^1.6.1"
  }
}
